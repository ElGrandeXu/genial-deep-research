Environment: detached clean worktree at commit `400f2fa`
Date: 2026-08-28 00:11 CEST

Command:

```powershell
corepack pnpm verify
```

Raw result:

```text
SOURCE_INTEGRITY_OK: 3 files
FOUNDATION_VERIFY_FAILED: excluded handoff missing: PASSATION_CHATGPT_GENIAL_2026-08-26.md
Exit 1
```

Cause: the cumulative verifier required two deliberately ignored handoff files
to exist. A clean clone cannot contain them, while the release rules explicitly
forbid adding them to the package.

Decision: continue proving that both paths are ignored and untracked, but make
their local presence optional. This removes no safety check relevant to a clone.

Follow-up at commit `2f0de74`:

```text
corepack pnpm verify -> Exit 0; 478/478 unit tests; 8/8 browser tests
git status --short -> M docs/evidence/audit-01-upgrade/screenshots/complete-1440.png
```

Cause: the browser suite rewrote tracked PNG evidence on every ordinary
verification; PNG bytes are not deterministic across captures. Decision: keep
responsive assertions in every run, but regenerate release captures only when
`UPDATE_RELEASE_EVIDENCE=1` is explicit.
